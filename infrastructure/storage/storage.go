// Package storage provides persistent storage implementations for Chat objects.
// Domain entities (Chat, Session, etc.) and the Storage interface are defined
// in GPTBot/domain/chat. This package contains only the concrete implementations
// (FileStorage, MemoryStorage) and the factory function.
package storage
